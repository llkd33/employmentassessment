# 포트 설정
PORT=5000

# 데이터베이스 설정 (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/employee_assessment

# JWT 토큰 암호화 키 (임의의 긴 문자열로 변경하세요)
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# 카카오 API 키 (선택사항 - 카카오 로그인 사용 시)
KAKAO_JAVASCRIPT_KEY=your_kakao_javascript_key_here

# 환경 모드
NODE_ENV=development

# === 로컬 개발용 기본값 ===
# 위의 값들을 실제 환경에 맞게 수정하고 .env 파일로 저장하세요.
# 
# PostgreSQL 설치가 안 되어 있다면:
# - Windows: https://www.postgresql.org/download/windows/
# - Mac: brew install postgresql
# - Linux: sudo apt-get install postgresql
#
# 데이터베이스 생성:
# 1. PostgreSQL 실행
# 2. createdb employee_assessment
# 3. 위의 DATABASE_URL을 실제 정보로 수정 